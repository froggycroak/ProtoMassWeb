# ProtoMass Web

由内网仓库 `pm` 的 `src/appserver/wwwroot` **对外页面**同步而来。

- 同步脚本：`pm_v207/scripts/sync-public-pages.ps1`
- 封面在站点根 `/`（`index.html`）；旧 `/portal/` 会跳回首页
- 大文件下载仍由内网 `DownloadsRoot` / Release 提供；本站 downloads 页多为索引与说明


